{{- if .Values.demoWeb.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "bc-wallet.fullname" . }}-demo-web
  labels:
    app.kubernetes.io/component: demo-web
    {{- include "bc-wallet.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "bc-wallet.fullname" . }}-demo-web
  minReplicas: {{ .Values.demoWeb.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.demoWeb.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.demoWeb.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.demoWeb.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.demoWeb.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.demoWeb.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
