{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-ingress
  namespace: {{ .Values.global.namespaceOverride | default .Release.Namespace }}
  labels:
    app.kubernetes.io/part-of: bc-wallet-showcase-builder
    {{- include "bc-wallet-showcase-builder.labels" . | nindent 4 }}
  annotations:
    {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  rules:
  - host: {{ include "bc-wallet-showcase-builder.api-server.host" . | quote }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: {{ $.Release.Name }}-{{ $.Values.api_server.name }}
            port:
              number: {{ $.Values.api_server.service.port }}
  - host: {{ include "bc-wallet-showcase-builder.traction-adapter.host" . | quote }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: {{ $.Release.Name }}-{{ $.Values.traction_adapter.name }}
            port:
              number: {{ $.Values.traction_adapter.service.port }}
  - host: {{ include "bc-wallet-showcase-builder.demo-web.host" . | quote }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: {{ $.Release.Name }}-{{ $.Values.demo_web.name }}
            port:
              number: {{ $.Values.demo_web.service.port }}
  - host: {{ include "bc-wallet-showcase-builder.showcase-creator.host" . | quote }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: {{ $.Release.Name }}-{{ $.Values.showcase_creator.name }}
            port:
              number: {{ $.Values.showcase_creator.service.port }}
  - host: {{ include "bc-wallet-showcase-builder.demo-server.host" . | quote }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: {{ $.Release.Name }}-{{ $.Values.demo_server.name }}
            port:
              number: {{ $.Values.demo_server.service.port }}
  {{- with .Values.ingress.tls }}
  tls:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}