{{- if .Values.demoServer.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "bc-wallet.fullname" . }}-demo-server
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/component: demo-server
    {{- include "bc-wallet.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/component: demo-server
      {{- include "bc-wallet.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
{{- if and .Values.ingress.enabled .Values.demoServer.networkPolicy.ingress.enabled }}
      {{- with .Values.demoServer.networkPolicy.ingress.namespaceSelector }}
    - from:
        - namespaceSelector:
            matchLabels:
{{ toYaml . | indent 14 }}
      {{- end }}
      {{- with .Values.demoServer.networkPolicy.ingress.podSelector }}
    - from:
        - podSelector:
            matchLabels:
{{ toYaml . | indent 14 }}
      {{- end }}
    {{- end }}
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/component: api-server
              {{- include "bc-wallet.selectorLabels" . | nindent 14 }}
{{- end }}
