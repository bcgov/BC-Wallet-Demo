1. Get the web UI URL (in-cluster or via ingress):
{{- if .Values.ingress.enabled }}
   https://{{ .Values.ingress.web.hostname }}{{ .Values.ingress.web.path }}
{{- else }}
   kubectl --namespace {{ include "showcase.namespace" . }} port-forward svc/{{ include "showcase.web.fullname" . }} 3000:3000
   Then open http://127.0.0.1:3000
{{- end }}

2. API service (ClusterIP):
   {{ include "showcase.server.fullname" . }}:5000

{{- if .Values.mongodb.enabled }}

3. MongoDB is enabled (CloudPirates chart). Set `mongodb.auth.rootPassword` to match the generated `MONGODB_URI` on the server.
   Primary host from this release: {{ include "showcase.mongodb.host" . }}:27017
{{- end }}

Reference: https://github.com/openwallet-foundation/helm-charts/tree/main/charts/acapy (CloudPirates OCI dependencies)
