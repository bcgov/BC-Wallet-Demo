apiVersion: v1
kind: Service
metadata:
  name: {{ include "bc-wallet-showcase.fullname" . }}-demo-server
  labels:
    app.kubernetes.io/component: demo-server
    {{- include "bc-wallet-showcase.labels" . | nindent 4 }}
spec:
  type: {{ .Values.demoServer.service.type }}
  ports:
    - port: {{ .Values.demoServer.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/component: demo-server
    {{- include "bc-wallet-showcase.selectorLabels" . | nindent 4 }}
