apiVersion: v1
kind: Service
metadata:
  name: {{ include "bc-wallet-showcase.fullname" . }}-traction-adapter
  labels:
    app.kubernetes.io/component: traction-adapter
    {{- include "bc-wallet-showcase.labels" . | nindent 4 }}
spec:
  type: {{ .Values.tractionAdapter.service.type }}
  ports:
    - port: {{ .Values.tractionAdapter.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/component: traction-adapter
    {{- include "bc-wallet-showcase.selectorLabels" . | nindent 4 }}
