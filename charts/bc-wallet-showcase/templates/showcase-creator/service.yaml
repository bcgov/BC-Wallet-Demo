apiVersion: v1
kind: Service
metadata:
  name: {{ include "bc-wallet-showcase.fullname" . }}-showcase-creator
  labels:
    app.kubernetes.io/component: showcase-creator
    {{- include "bc-wallet-showcase.labels" . | nindent 4 }}
spec:
  type: {{ .Values.showcaseCreator.service.type }}
  ports:
    - port: {{ .Values.showcaseCreator.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/component: showcase-creator
    {{- include "bc-wallet-showcase.selectorLabels" . | nindent 4 }}
