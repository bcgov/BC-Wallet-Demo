apiVersion: v1
kind: Service
metadata:
  name: {{ include "bc-wallet-showcase.fullname" . }}-api-server
  labels:
    app.kubernetes.io/component: api-server
    {{- include "bc-wallet-showcase.labels" . | nindent 4 }}
spec:
  type: {{ .Values.apiServer.service.type }}
  ports:
    - port: {{ .Values.apiServer.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/component: api-server
    {{- include "bc-wallet-showcase.selectorLabels" . | nindent 4 }}
